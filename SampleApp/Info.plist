<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Registers the notchdemo:// scheme — the half of the Notchboard integration that
	     can't be expressed through generated-Info.plist build settings. The rest of the
	     Info.plist is generated (GENERATE_INFOPLIST_FILE = YES) and merged with this. -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>flourix.notchdemo.deeplink</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>notchdemo</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
